# requirements.txt
#
# Core dependencies for the quantum-resistant-encryption-playground.
# To regenerate pins: pip freeze | grep -E "numpy|scipy|scikit-learn|matplotlib|pandas" > requirements.txt

# Numerical and Scientific Computing
numpy>=1.24,<3
scipy>=1.10,<2

# Machine Learning (used by ai/ analysis scripts)
scikit-learn>=1.3,<2

# Data Visualization
matplotlib>=3.7,<4

# Data Handling
pandas>=2.0,<3

# Post-Quantum Cryptography (ML-KEM key encapsulation)
kyber-py>=1.0,<2

# Optional: Quantum Computing (needed for quantum/ simulations)
# Install separately if you want to run Grover's/Shor's examples:
#   pip install qiskit>=1.0
